[pytest]
testpaths = tests
pythonpath =
    wazuh-llm-bridge
addopts = -ra
markers =
    unit: fast tests with no network services
    integration: tests using local fake HTTP services only
    model: tests that require a real LM Studio-compatible model endpoint
    e2e: tests that require the Wazuh Docker stack
    manual: interactive tests that require human verification
