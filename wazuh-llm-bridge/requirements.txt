fastapi>=0.110
httpx>=0.27
uvicorn[standard]>=0.29

# Phase 2: interactive Slack buttons via Socket Mode (no public URL needed)
# aiohttp is slack-bolt's transport for AsyncApp — must be installed explicitly
slack-bolt>=1.18
slack-sdk>=3.27
aiohttp>=3.9

# Phase 4: org_profile.yaml (business context injected into LLM prompt)
# python-multipart is required by FastAPI's Form() handlers in /admin
PyYAML>=6.0
python-multipart>=0.0.9
