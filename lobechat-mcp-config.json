{
  "_comment": "Paste this into LobeChat → Settings → Tools / MCP → Custom MCP Server. Replace YOUR_API_KEY with the value printed by install.sh (or the one you set as MCP_API_KEY in wazuh-mcp/.env). If LobeChat runs in Docker, change localhost to host.docker.internal.",
  "mcpServers": {
    "wazuh": {
      "type": "http",
      "url": "http://localhost:3000/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
